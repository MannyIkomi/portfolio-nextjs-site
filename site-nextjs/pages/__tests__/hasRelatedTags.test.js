// import { hasRelatedTags } from '../projects'

describe('getRelatedProjects()', () => {
  const hasRelatedTags = (currentProject, otherProject) => {
    return 'test'
  }
  it.todo('removes the current project from the list')
  it.todo('finds where current tags match other tags')
  it('returns test', () => {
    expect(hasRelatedTags()).toEqual('test')
  })
})
